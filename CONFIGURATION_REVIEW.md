# mirs_ble_app 構成レビューと修正計画

## 目的

`mirs_ble_app` の構成上の問題を整理し、優先度順に修正するための作業基準とする。

現状は、`flutter analyze` と既存テストは通過している。一方で、BLE 実機接続の契約、本番配布設定、テスト範囲には未整備な点がある。

## 問題一覧

### 1. BLE 検出条件と接続条件が矛盾している — 高

**対応済み（対象条件をサービスUUIDに限定）**

- 対象: `lib/ble_service.dart:101-103`
- 対象: `lib/ble_service.dart:124-137`
- 対象: `lib/ble_service.dart:146-156`
- スキャン開始時にサービスUUIDを `withServices` へ指定している。
- `_isTarget()` はデバイス名またはサービスUUIDの一致を許可している。
- しかし接続時には `platformName == "MIRS-Robot"` を必須としている。
- 接続対象をサービスUUID一致のデバイスだけに限定した。
- スキャン開始時にもサービスUUIDフィルタを指定し、無関係なデバイスへの接続試行を防止した。

### 2. READMEと実装の接続条件が一致していない — 高

- 対象: `README.md:19-20`
- READMEでは、デバイス名に `MIRS` を含むデバイスを対象としている。
- 実装では `MIRS-Robot` との完全一致しか許可していない。
- ドキュメントを信じて設定したデバイス名では接続できない可能性がある。

### 3. リリースビルドがデバッグ署名のまま — 高

- 対象: `android/app/build.gradle.kts:31-35`
- releaseビルドがデバッグキーで署名されている。
- Play Store配布や正式配布に利用できない。
- 将来のアップデートで同じ署名を維持できる本番鍵の管理も未設定である。

### 4. BLE書き込みのMTU・分割・応答確認がない — 高

- 対象: `lib/ble_service.dart:220-225`
- JSON全体を1回のCharacteristic Writeで送信している。
- JSONがMTUを超えた場合の分割送信がない。
- 受信側のACKや処理結果を確認していない。
- 座標数の増加や受信側実装の変更で、送信失敗やデータ欠損が起きる可能性がある。

### 5. Android権限要求が一律で不要な権限を含む — 中

- 対象: `lib/main.dart:105-110`
- Bluetooth権限と位置情報権限を常に要求している。
- Manifestには `BLUETOOTH_ADVERTISE` も宣言されているが、現状はBLEクライアントとしてスキャン・接続・書き込みを行うだけである。
- Androidバージョンに応じた権限整理がなく、不要な権限要求によって初回利用時の拒否を招く可能性がある。

### 6. スキャン処理の構造と例外境界が不十分 — 中

- 対象: `lib/ble_service.dart:77-121`
- `startScan(timeout: 8秒)` の完了を待ってから、検出結果を取得している。
- 対象デバイスを早期検出しても、接続開始まで待ち時間が発生する可能性がある。
- `adapterState.first` が `try` の外にあり、状態ストリームの例外処理が統一されていない。

### 7. 清掃範囲モデルの検証が弱い — 中

- 対象: `lib/models/cleaning_zone.dart`
- 頂点数が3以上であることと、座標が有限値であることしか検証していない。
- 重複点、面積0、自己交差、頂点順序の不正、地図範囲外を許可する。
- UIは四角形を要求しているが、モデルは任意の3頂点以上のポリゴンを許可しており、仕様が一致していない。

### 8. BLE主要機能の自動テストがない — 中

- 対象: `test/widget_test.dart`
- 現在のテストは画面表示とモデルの基本的なシリアライズ・検証に限られている。
- スキャン条件、接続、切断後の再接続、Characteristic欠落、送信失敗をカバーしていない。
- BLE仕様変更時の回帰を検出しにくい。

### 9. `main.dart` に責務が集中している — 中

- 対象: `lib/main.dart`
- 画面表示、権限要求、フォーム状態、ログ管理、BLE開始処理が1つのStatefulWidgetに集約されている。
- 変更時の影響範囲が大きく、単体テストが難しい。
- `BleService`、権限管理、画面状態を分離した方が保守しやすい。

### 10. サービス配置と地図ロジックに重複がある — 低

- `lib/ble_service.dart` は `lib/` 直下にある。
- `map_selection_controller.dart` は `lib/services/` にある。
- `MapGeometry.computeMapHeight()` と `MapSelectionArea` 内の地図高さ計算が重複している。
- 未使用または利用経路が不明なメソッドもあり、将来の修正時に不整合が起きやすい。

## 修正順序

### Step 1: BLEの検出・接続条件を統一する — 完了

- [x] 正式な対象条件を決める（サービスUUID一致のみ）。
- [x] サービスUUID一致時にデバイス名を必須にしない。
- [x] デバイス名を接続対象の判定から除外する。
- [x] READMEの接続条件を実装に合わせて更新する。
- [x] 対象外デバイスが接続されない条件を確認する。

**完了条件**

- UUID一致のデバイスに接続できる。
- 名前一致のデバイスに接続できる。
- 対象外デバイスには接続しない。
- READMEの説明と実装が一致している。

### Step 2: BLE送信プロトコルを堅牢化する — 完了

- [x] 接続後にCharacteristicの書き込み特性を確認する。
- [x] 使用可能なMTUを取得する。
- [x] MTUを超えるJSONの分割方法を決める。
- [x] メッセージ終端またはフレーム形式を決める。
- [x] ACK、NACK、タイムアウトの扱いを決める。
- [x] 送信中の二重送信を防ぐ。

**今回の対応**

- 書き込み不可のCharacteristicには接続完了状態にしない。
- Android接続時にMTU 247 bytesを要求し、失敗時は現在のMTUを使用する。
- MTUを超える送信はATT payload単位に分割して送信する。
- 512 bytesを超える送信は、受信側の分割プロトコルが未定義のため拒否する。
- JSON UTF-8バイト列をATT payload単位に分割し、各チャンクをWrite with Responseで順番に送信する。
- メッセージ終端には改行（`LF`）を使用し、受信側で終端まで結合してからJSON解析する。
- 1メッセージの上限は4096 bytesとする。
- Response Characteristic（`...9abe`）を追加し、受信側の成功を`ACK`、検証失敗を`NACK`で返す。
- アプリは最終チャンク送信後5秒以内にResponse Characteristicを読み取り、`ACK`以外を送信失敗として扱う。

**完了条件**

- 通常サイズのミッションを確実に送信できる。
- MTUを超えるミッションを仕様どおり送信できる。
- 受信側の成功・失敗をアプリで判定できる。

### Step 3: Android権限を整理する

- [x] Androidのバージョン別に必要な権限を整理する。
- [x] BLEクライアントに不要な `BLUETOOTH_ADVERTISE` を削除する。
- [x] Android 11以下だけ位置情報権限を要求する。
- [x] 権限を恒久拒否した場合に設定画面への導線を追加する。
- [x] BluetoothがOFFの場合の案内を維持する。

**完了条件**

- 必要最小限の権限だけを要求する。
- 権限拒否時にユーザーが復旧方法を理解できる。
- 対応Androidバージョンでスキャンできる。

### Step 4: 本番配布設定を整える — 完了

- [x] release用署名鍵の設定手順を用意する。
- [x] 署名情報をソースコードへ直接記載しない。
- [x] `key.properties` とkeystoreを `.gitignore` 対象にする。
- [x] releaseビルドでdebug署名を使わないようにする。
- [x] アプリ名、application ID、バージョン設定を確認した。

**完了条件**

- `android/key.properties` が設定されている環境ではrelease APK/AABが本番鍵で署名される。
- 署名鍵やパスワードがリポジトリに含まれない。
- 同じ署名でアップデート可能な手順が文書化されている。

### Step 5: 清掃範囲の仕様と検証を統一する — 完了

- [x] 任意ポリゴンを許可する仕様に統一する。
- [x] 最小面積を `0.0001 m²` と定義する。
- [x] 重複点・自己交差・頂点順序を検証する。
- [x] 座標の絶対値を `1000 m` 以下に制限する。
- [x] FlutterモデルとROS 2受信側で同じ検証ルールを利用する。

**完了条件**

- UIで作成した範囲が必ず有効なミッションになる。
- 不正な範囲を送信できない。
- 受信側の座標仕様と一致している。

### Step 6: テストを追加する

- [x] BLE対象デバイス判定のテストを追加する。
- [x] Characteristic欠落時のテストを追加する。
- [x] 切断後の再接続条件テストを追加する。
- [x] 送信成功・送信失敗・不正応答のロジックテストを追加する。
- [x] 清掃範囲の境界値・不正形状のテストを追加する。
- [x] 権限拒否結果が設定導線へ渡ることをテストする。

**完了条件**

- BLE仕様の主要な分岐が自動テストで保護されている。
- 仕様変更時に接続・送信の回帰を検知できる。

### Step 7: 責務とディレクトリ構成を整理する — 完了

- [x] `BleService` を `lib/services/ble_service.dart` へ移す。
- [x] 権限要求を `PermissionService` へ分離する。
- [x] 画面状態は既存のStatefulWidgetで管理し、通信・権限ロジックを画面から分離する。
- [x] 地図高さの計算を `MapGeometry` に集約する。
- [x] 未使用の地図コントローラーメソッドを削除する。

**完了条件**

- `main.dart` が画面構築中心になる。
- 通信ロジックを画面なしでテストできる。
- 座標変換・表示サイズの計算元が一つになる。

## 検証コマンド

プロジェクトルート（`mirs_ble_app`）で実行する。

```sh
flutter pub get
flutter analyze
flutter test
```

Androidのrelease設定を変更した場合は、署名設定を確認したうえで次も実行する。

```sh
flutter build apk --release
```

## 現時点の検証結果

- `flutter analyze`: 成功
- `flutter test`: 成功
- 既存テスト: 12件
